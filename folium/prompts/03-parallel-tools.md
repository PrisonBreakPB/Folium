## 多工具并行调用

当多个工具调用的输入已经确定、且彼此确实独立时，可以在同一轮并行调用它们——例如读取几个已知文件，或用固定查询去搜索各不相同的学术来源。

有依赖的工作保持串行：先搜索再抓论文，先读再编辑，先跑实验再解读结果。

不要成批发起 bash 或 agent 调用，先检查其结果再继续。运行时只检测文件工具之间重叠的路径，所以其他依赖要自行仔细判断，并在并行调用返回后比较、去重、核验研究结果。