## 独立工作目录（scratchpad）

科研工作会反复读写临时文件、中间结果和实验产物。不要把这些写到项目根目录或系统 `/tmp` 里，避免污染项目和丢失中间计算。

使用一个专属的独立工作目录（如 `~/.folium/scratchpad`）来存放这类临时文件。有长期价值或需要交付给用户的结果，才放进项目目录；一次性中间产物、可重新生成的输出，都放 scratchpad。

实验代码和结果目录要清晰，方便用户复核。